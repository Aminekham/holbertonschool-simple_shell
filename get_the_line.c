#include "main.h"
char *get_the_line(void)
{
    int len, i;
    char *userinput;

    userinput = (char *)malloc(sizeof(char) * len);
    if(userinput==NULL)
    {
        perror("")
    }
    while ((/* condition */))
    {
        /* code */
    }
    return (userinput);
}