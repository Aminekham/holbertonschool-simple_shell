#include "main.h"
/**
 * first_step
 * Return:void.
 */
void first_step(void)
{
    int status = 1;
  char* readline; 

    while (status)
    {
        printf("$");
        /**wait for user to enter commands */

        if (status = 0)
        {
            break;
        }
    }
}